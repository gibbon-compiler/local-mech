# Dead-code allowlist for find_dead_code.py
#
# Each name listed here is excluded from the dead-code check.
# Items are added here when they are intentionally kept despite being
# unreferenced (e.g. top-level theorems that are the final result of the
# development, or illustrative examples), or when they are known pre-existing
# dead code that has not yet been cleaned up.
#
# Workflow:
#   • When you prove a new lemma/definition that is legitimately "top-level"
#     (i.e. a main result rather than a helper), add it here with a comment.
#   • When cleaning up old dead code, remove the corresponding entry.
#   • New dead helpers introduced by AI / refactoring that are NOT listed here
#     will cause CI to fail — that is the intended behaviour.
#
# Format: one identifier per non-blank, non-comment line.

# -------------------------------------------------------------------------
# Top-level theorems (the primary results of the mechanization).
# These are intentionally unreferenced by other code — they are the goal.
# -------------------------------------------------------------------------
preservation
type_safety

# -------------------------------------------------------------------------
# Illustrative examples embedded in the source files.
# These demonstrate the type system / semantics but are not used in proofs.
# -------------------------------------------------------------------------
ex_tree_alloc
ex_tree_case
ex_eval_tree
ex_end_witness_tree
ex_step_by_step_typing

# -------------------------------------------------------------------------
# Pre-existing dead code — known at the time this check was introduced.
# Clean these up and remove the entries as part of normal maintenance.
# -------------------------------------------------------------------------

# LoCalSyntax.v
tycon_eq_dec
term_var_name
loc_var_name
region_var_name
pat_bound_loc_vars
expr_bound_loc_vars
program_symbolic_only
app_subst_fresh
program_alpha_regular

# LoCalStatic.v
params_to_tenv
type_mentions_loc
fresh_laddr_store
fresh_laddr_conloc

# LoCalDynamic.v
subst_val_fresh
subst_vals_fresh
subst_locs_fresh
subst_app_fresh

# LoCalSafety.v
In_lookup_fdecl
alloc_extends_refl
alloc_extends_trans
alloc_extends_extend_alloc
alloc_env_functional_singleton
runtime_nursery_wf_remove_nursery
store_wf_remove_nursery
tenv_equiv_extend_shadow
closed_value_typing_any_context
typed_value_nil_no_term_vars
gamma_binders_disjoint_nil
expr_symbolic_only_expr_wf_any
expr_alpha_regular_let_inv
fdecl_alpha_regular_inv
expr_term_capture_safe_let_inv
fdecl_instantiation_fresh_ok
pats_have_type_fresh_In
end_witness_fields_functional
end_witness_fields_store_write_other_region
pat_has_type_tenv_equiv
pats_have_type_tenv_equiv
runtime_nursery_wf_letregion
runtime_nursery_wf_letloc_start
runtime_nursery_wf_letloc_tag
runtime_nursery_wf_letloc_after
pat_has_type_sigma_c_monotone
pats_have_type_sigma_c_monotone
existsb_bind_miss
runtime_nursery_wf_case
nodup_pat_laddrs_head_miss
subst_case_pats_eq
typed_nil_value_has_type_fresh_any_context
substitution_val_fresh_paired
fresh_subject_reduction_closed
expr_wf_subject_reduction_closed
